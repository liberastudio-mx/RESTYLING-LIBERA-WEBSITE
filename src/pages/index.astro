---
import Layout from '@/layouts/Layout.astro';
import { Image } from 'astro:assets';

import problemasImg    from '@/assets/images/LS - problemas.png';
import solOperacionImg from '@/assets/images/LS - 2 - operacion inteligente.png';
import solPresenciaImg from '@/assets/images/LS - 2 - presencia digital.png';
import solAiImg        from '@/assets/images/LS - 2 - Ai e automation.png';
import solProductosImg from '@/assets/images/LS - 2 - Productos digitales.png';
import logoCenotesImg  from '@/assets/images/logo cenotes.png';
import logoPaosImg     from '@/assets/images/Logo paos.png';
import logoSoleFacileImg from '@/assets/images/logo sole facile per linkedin.png';
import logoMorozzoImg  from '@/assets/images/logo morozzo.png';
import radarBgImg      from '@/assets/images/LS - Background radar.png';
import radar1Img       from '@/assets/images/LS - radar 1.png';
import radar2Img       from '@/assets/images/LS - radar 2.png';
import radar3Img       from '@/assets/images/LS - radar 3.png';
import ctaBgImg        from '@/assets/images/LS - background CTA.png';

const solucionesHome = [
  {
    id: 'operacion',
    label: 'Operación Interna',
    promise: 'Para operar con orden, no con memoria.',
    description: 'PAOS, CRM simple, dashboards internos y automatización de procesos. Cada mensaje, lead y tarea en su lugar.',
    href: '/soluciones/operacion-interna',
    accent: '#003049',
    image: solOperacionImg,
    services: ['Second Brain/PAOS', 'CRM y leads', 'Dashboards', 'Integraciones'],
  },
  {
    id: 'presencia',
    label: 'Presencia Digital',
    promise: 'Para que te encuentren, te entiendan y te elijan.',
    description: 'Sitios web, landing pages, Google Business Profile, WhatsApp Business y redes sociales. Tu primera impresión, trabajando 24/7.',
    href: '/soluciones/presencia-digital',
    accent: '#d62828',
    image: solPresenciaImg,
    services: ['Sitios web premium', 'Landing pages', 'Redes sociales', 'Google Business'],
  },
  {
    id: 'ai',
    label: 'AI & Automation',
    promise: 'IA aplicada a procesos reales, no a demos.',
    description: 'Agentes de IA, asistentes internos, clasificación de mensajes y flujos con n8n. Automatizamos donde hay fricción, no donde hay moda.',
    href: '/soluciones/ai-automation',
    accent: '#f77f00',
    image: solAiImg,
    services: ['Agentes de IA', 'Asistentes internos', 'n8n & APIs', 'Automatización documental'],
  },
  {
    id: 'productos',
    label: 'Productos Digitales',
    promise: 'Herramientas que tu equipo realmente usa.',
    description: 'Web apps, MVPs, paneles administrativos y prototipos funcionales. Del problema al producto, sin años de desarrollo.',
    href: '/soluciones/productos-digitales',
    accent: '#fcbf49',
    image: solProductosImg,
    services: ['Web apps', 'MVPs', 'Apps internas', 'Prototipos'],
  },
];
---

<Layout
  title="LIBERA Studio — Estudio de sistemas digitales"
  description="Sitios web, automatizaciones, agentes de IA e integraciones que conectan tu presencia online con la operación real de tu negocio. Mérida, México."
>

  <!-- ═══════════════════════════════════════════════════════════════════
       1. HERO, Full-bleed dark, A como núcleo visual
       ═══════════════════════════════════════════════════════════════════ -->
  <div class="hero-pin-stage">
  <section
    id="hero"
    class="hero-panel relative flex flex-col justify-center min-h-[100dvh] overflow-hidden"
    style="background: #ffffff;"
    aria-label="Hero"
  >
    <!-- DotField interactive background -->
    <div id="hero-dot-field" class="absolute inset-0 pointer-events-none" aria-hidden="true"></div>

    <!-- Content -->
    <div class="container-libera relative z-10 py-20">

      <!-- Headline -->
      <h1
        class="mb-8"
        style="color: #003049; font-family: var(--font-display); font-weight: 700; letter-spacing: -0.02em; line-height: 1.05; font-size: clamp(1.75rem, 2.8vw, 3rem);"
        data-reveal
        data-hero-headline
      >
        <span data-hero-line style="display:block; white-space:nowrap;">Construimos</span>
        <span data-hero-line style="display:block; white-space:nowrap;">el sistema que</span>
        <span data-hero-line style="display:block; white-space:nowrap;"><em style="color: var(--orange); font-style: italic;">libera</em> tu negocio.</span>
      </h1>

      <!-- Subheadline -->
      <p
        class="mb-10 max-w-xl text-lg leading-relaxed"
        style="color: #003049; opacity: 0.7; transition-delay: 110ms;"
        data-reveal
      >
        Presencia digital, automatización e inteligencia artificial.
      </p>

    </div>

    <!-- CTAs — anchored to bottom -->
    <div class="container-libera absolute bottom-10 left-0 right-0 flex flex-wrap items-center gap-4" data-reveal style="transition-delay: 180ms;">
      <a href="/diagnostico" class="btn btn-primary">
        Solicitar diagnóstico
        <span class="btn-icon" aria-hidden="true">→</span>
      </a>
      <a href="/trabajo" class="btn btn-ghost-light">
        Trabajos
      </a>
    </div>

  </section>
  </div>


  <!-- ═══════════════════════════════════════════════════════════════════
       2. PROBLEMA, Full-bleed light, testo grande
       ═══════════════════════════════════════════════════════════════════ -->
  <div class="problem-pin-stage">
  <section
    id="problema"
    style="background: #ffffff;"
    class="problem-overlap"
    aria-label="El problema que resolvemos"
  >
    <!-- Left: text + stats -->
    <div class="problem-left" data-reveal>
      <div class="problem-content">
        <h2 class="problem-title mb-6" style="color: var(--text-primary);">
          Tecnología que se integra a <em style="color: var(--orange); font-style: italic;">tu forma de trabajar.</em>
        </h2>
        <p class="problem-body mb-4" style="color: var(--text-secondary);">
          Una buena web, un WhatsApp bien configurado y procesos claros pueden cambiar la manera en que un negocio atiende, vende y crece.
        </p>
        <p class="problem-body" style="color: var(--text-secondary);">
          LIBERA diseña sistemas digitales simples, conectados y útiles: pensados para que tu equipo los use de verdad.
        </p>
      </div>

      <div class="problem-mobile-visual">
        <Image
          src={problemasImg}
          alt="Sistemas digitales integrados"
          widths={[400, 720]}
          sizes="100vw"
          loading="lazy"
        />
      </div>

      <div class="problem-stats">
        {[
          {
            value: '31%',
            text: 'de reducción en costos de TI atribuidos a automatización inteligente.',
            source: 'IBM Institute for Business Value',
          },
          {
            value: '0.5–3.4 pts',
            text: 'de crecimiento anual de productividad podrían venir de IA generativa y automatización.',
            source: 'McKinsey',
          },
          {
            value: '2.6x',
            text: 'más citas reportó Farmacias del Ahorro usando WhatsApp Flows.',
            source: 'WhatsApp Business',
          },
          {
            value: '51%',
            text: 'más interacciones reportó Banco Ciudad usando WhatsApp Business Platform.',
            source: 'WhatsApp Business',
          },
        ].map((stat) => (
          <div class="problem-stat">
            <p class="problem-stat-value font-display" style="font-family: var(--font-display);">{stat.value}</p>
            <p class="problem-stat-text">{stat.text}</p>
            <p class="problem-stat-source">Fuente: {stat.source}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- Right: image full height -->
    <div class="problem-right">
      <Image
        src={problemasImg}
        alt="Sistemas digitales integrados"
        widths={[720, 1080]}
        sizes="50vw"
        loading="lazy"
        class="problem-img"
      />
    </div>
  </section>
  </div>


  <!-- ═══════════════════════════════════════════════════════════════════
       3. SOLUCIONES, Full-bleed con 4 bloques colorati
       ═══════════════════════════════════════════════════════════════════ -->
  <section
    id="soluciones"
    data-solutions-section
    style="background: var(--surface);"
    class="solutions-overlap pb-24 lg:pb-32"
    aria-label="Nuestras soluciones"
  >
    <div class="container-libera solutions-pin">
      <div class="solutions-layout">

        <!-- Left: full-height sticky image -->
        <div class="solutions-image-col">
          <figure class="solution-visual-shell" data-reveal>
            <div class="solution-visual-frame">
              {solucionesHome.map((sol, i) => (
                <Image
                  src={sol.image}
                  alt={sol.label}
                  widths={[600, 900, 1340]}
                  sizes="(max-width: 1023px) 100vw, 50vw"
                  class={`solution-visual-image ${i === 0 ? 'is-active' : ''}`}
                  data-solution-image={sol.id}
                  loading={i === 0 ? 'eager' : 'lazy'}
                />
              ))}
            </div>
          </figure>
        </div>

        <!-- Right: title + cards -->
        <div class="solutions-sticky">
          <div class="solutions-intro" data-reveal>
            <h2 class="display-lg solutions-title" style="color: var(--text-primary);">
              Diseñamos <em style="color: var(--orange); font-style: italic;">los sistemas que conectan</em> presencia, operación e inteligencia.
            </h2>
          </div>

          <div class="solutions-card-rail" aria-label="Lista de soluciones">
          {solucionesHome.map((sol, i) => (
            <a
              href={sol.href}
              class={`solution-card group ${i === 0 ? 'is-active is-visible' : ''}`}
              data-solution-card={sol.id}
              style={`--solution-accent: ${sol.accent};`}
            >
              <div class="solution-card-mobile-visual" aria-hidden="true">
                <Image
                  src={sol.image}
                  alt=""
                  widths={[400, 800]}
                  sizes="100vw"
                  loading="lazy"
                />
              </div>
              <div class="solution-card-glow" aria-hidden="true"></div>
              <span class="solution-card-index" aria-hidden="true">0{i + 1}</span>

              <div class="relative">
                <div class="flex items-center justify-end gap-3 mb-8">
                  <span class="text-xs font-semibold tracking-widest uppercase" style={`color: ${sol.accent};`}>
                    {sol.label}
                  </span>
                  <span class="w-3 h-3 rounded-full flex-shrink-0" style={`background: ${sol.accent};`} aria-hidden="true"></span>
                </div>

                <h3 class="solution-card-title">
                  {sol.promise}
                </h3>

                <p class="solution-card-copy">
                  {sol.description}
                </p>

                <div class="flex flex-wrap justify-end gap-2">
                  {sol.services.map((s) => (
                    <span class="solution-service-tag">
                      {s}
                    </span>
                  ))}
                </div>
              </div>

              <div class="relative mt-10 flex items-center justify-end gap-3 text-sm font-semibold" style={`color: ${sol.accent};`}>
                <span>Ver solución</span>
                <span class="solution-card-arrow" aria-hidden="true">→</span>
              </div>
            </a>
          ))}
          </div>
        </div>
      </div>
    </div>
  </section>


  <!-- ═══════════════════════════════════════════════════════════════════
       4. MÉTODO, Editorial Split, sfondo scuro
       ═══════════════════════════════════════════════════════════════════ -->
  <section
    id="metodo"
    data-method-clone-section
    style="background: #120F17;"
    class="method-clone-scroll"
    aria-label="Nuestro método"
  >
    <div class="container-libera method-clone-sticky">
      <div class="method-clone-layout">
        <div class="method-clone-copy">
          <h2
            class="font-display method-clone-title mb-8"
            data-method-clone-title
            style="
              font-family: var(--font-display);
              font-weight: 700;
              letter-spacing: 0.015em;
            "
          >
            <span class="method-clone-title-line" data-method-clone-title-line><span>Primero </span><span class="method-clone-title-action">ordenamos</span><span>.</span></span>
            <span class="method-clone-title-line" data-method-clone-title-line><span>Después </span><span class="method-clone-title-action">diseñamos</span><span>.</span></span>
            <span class="method-clone-title-line" data-method-clone-title-line><span>Luego </span><span class="method-clone-title-action">automatizamos</span><span>.</span></span>
          </h2>
        </div>

        <div class="method-clone-stage" data-method-clone-stage>
          {[
            {
              title: 'Diagnóstico',
              description: 'Revisamos cómo te encuentran, cómo respondes y dónde se pierde valor. Sin compromiso y sin jerga.',
              accent: 'var(--presencia)',
            },
            {
              title: 'Arquitectura',
              description: 'Definimos qué construir, en qué orden y por qué. Lo que no aporta, no entra.',
              accent: 'var(--operacion)',
            },
            {
              title: 'Diseño',
              description: 'Claridad antes que decoración. Cada pantalla existe para explicar, convencer o convertir.',
              accent: 'var(--orange)',
            },
            {
              title: 'Desarrollo',
              description: 'Código original, stack moderno, sistemas probados. Nada de templates disfrazados.',
              accent: 'var(--ai)',
            },
            {
              title: 'Integración',
              description: 'Conectamos la solución con tu operación real: WhatsApp, CRM, formularios, analytics.',
              accent: 'var(--productos)',
            },
            {
              title: 'Evolución',
              description: 'Medimos, ajustamos y crecemos el sistema. Un sistema vivo no se entrega: se cultiva.',
              accent: 'var(--operacion)',
            },
          ].map((step) => (
            <div
              class="method-clone-step"
              style={`--step-accent: ${step.accent};`}
              data-method-clone-step
            >
              <h3
                class="font-display mb-4"
                style="font-family: var(--font-display);"
                data-method-clone-step-title
              >
                {step.title}
              </h3>
              <p class="text-base leading-relaxed">
                {step.description}
              </p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════════════════════════════════════════════════════════════════
       5. TRABAJO, Eyebrow 2 di 3, card grid su sfondo chiaro
       ═══════════════════════════════════════════════════════════════════ -->
  <div class="work-pin-stage">
  <section
    id="trabajo"
    style="background: var(--surface); background-image: radial-gradient(oklch(10% 0.01 25 / 0.045) 1px, transparent 1px); background-size: 22px 22px;"
    class="work-overlap"
    aria-label="Nuestro trabajo"
  >
    <div class="container-libera w-full work-inner">
      <div class="work-header mb-5 flex-shrink-0" data-reveal>
        <div>
          <h2 class="display-md" style="color: var(--text-primary);">
            Construimos para clientes.<br/>
            <em style="color: var(--orange); font-style: italic;">También construimos para aprender.</em>
          </h2>
          <p class="text-sm leading-relaxed mt-3 max-w-xl" style="color: var(--text-secondary);">
            Cada proyecto, propio o de cliente, es un sistema en producción.
          </p>
        </div>
        <a
          href="/trabajo"
          class="work-ver-todo inline-flex items-center gap-2 text-sm font-bold transition-opacity duration-150 hover:opacity-70"
          style="color: var(--orange);"
        >
          Ver todo el trabajo <span aria-hidden="true">→</span>
        </a>
      </div>
      <div
        class="work-card-grid"
        style="background: var(--border); border: 1px solid var(--border); gap: 1px;"
      >
        {[
          {
            name: 'Cenote Scout',
            category: 'Producto digital',
            description: 'Plataforma para descubrir cenotes, rutas y experiencias en Yucatán. Sistema de datos, mapas y contenido editorial.',
            stack: ['Astro', 'TypeScript', 'Supabase'],
            status: 'En desarrollo',
            href: '/trabajo/cenote-scout',
            accent: '#f77f00',
            image: logoCenotesImg,
          },
          {
            name: 'Second Brain/PAOS',
            category: 'Sistema interno',
            description: 'Sistema operativo del estudio: 15 agentes de IA, automatizaciones, vault Obsidian y flujos n8n para operar sin fricción.',
            stack: ['Obsidian', 'Agents', 'Skills'],
            status: 'En producción',
            href: '/trabajo/radar',
            accent: '#fcbf49',
            image: logoPaosImg,
          },
          {
            name: 'Sole Facile',
            category: 'Cliente',
            description: 'Ecosistema digital para empresa de instalación solar en Italia. Web, contenido, automatizaciones y agente IA.',
            stack: ['WordPress', 'n8n', 'Claude API'],
            status: 'En producción',
            href: '/trabajo/sole-facile',
            accent: '#d62828',
            image: logoSoleFacileImg,
          },
          {
            name: 'Morozzo Padel',
            category: 'Cliente',
            description: 'Presencia digital y sistema de reservas para club de pádel. Rediseño, SEO local y automatización de comunicación.',
            stack: ['HTML/CSS', 'JS', 'WhatsApp API'],
            status: 'Completado',
            href: '/trabajo/morozzo-padel',
            accent: '#eae2b7',
            image: logoMorozzoImg,
          },
        ].map((project, i) => (
          <a
            href={project.href}
            class="group relative overflow-hidden flex flex-col justify-between work-card"
            style={`transition-delay: ${i * 65}ms;`}
            data-reveal
          >
            <Image
              src={project.image}
              alt=""
              aria-hidden="true"
              widths={[400, 700]}
              sizes="(max-width: 1023px) 100vw, 50vw"
              class="absolute inset-0 w-full h-full object-cover object-center transition-transform duration-700 ease-out group-hover:scale-105"
              loading="lazy"
            />
            <div
              class="absolute inset-0 transition-opacity duration-500 opacity-0"
              style="background: linear-gradient(to top, rgba(0,20,40,0.92) 0%, rgba(0,20,40,0.65) 50%, rgba(0,20,40,0.3) 100%);"
              aria-hidden="true"
              data-card-overlay
            ></div>
            <div class="relative z-10 flex flex-col justify-end h-full transition-opacity duration-500 opacity-0 group-hover:opacity-100 p-8">
              <h3
                class="font-display font-semibold text-2xl mb-4"
                style="font-family: var(--font-display); color: #ffffff; line-height: 1.2;"
              >
                {project.name}
              </h3>
              <div class="flex items-center justify-between gap-4">
                <div class="flex flex-wrap gap-2">
                  {project.stack.map((tech) => (
                    <span
                      class="text-xs font-mono px-2.5 py-1 rounded-md"
                      style="background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.6); font-family: var(--font-mono); border: 1px solid rgba(255,255,255,0.1);"
                    >
                      {tech}
                    </span>
                  ))}
                </div>
                <span
                  class="flex-shrink-0 text-2xl font-medium"
                  style="color: #fcbf49;"
                  aria-hidden="true"
                >→</span>
              </div>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
  </div>


  <!-- ═══════════════════════════════════════════════════════════════════
       6. RADAR, editoriale full-width
       ═══════════════════════════════════════════════════════════════════ -->
  <div class="conocimiento-pin-stage">
  <section
    id="conocimiento"
    aria-label="Radar"
  >
    <div class="conocimiento-half radar-feature" style="background: var(--ink); position: relative; overflow: hidden;">
      <Image
        src={radarBgImg}
        alt=""
        aria-hidden="true"
        widths={[800, 1440, 1920]}
        sizes="100vw"
        class="absolute inset-0 w-full h-full object-cover object-center"
        loading="lazy"
        style="opacity: 0.35;"
      />
      <div class="absolute inset-0" style="background: rgba(0,20,40,0.72);" aria-hidden="true"></div>

      <div class="container-libera w-full relative z-10">
        <div class="conocimiento-half-grid">

          <!-- Heading -->
          <div data-reveal class="flex flex-col justify-center pr-4" style="padding-block: 2.5rem;">
            <p class="eyebrow mb-3" style="color: #fcbf49; font-family: var(--font-mono);">Radar</p>
            <h2 class="display-lg mb-4" style="color: #ffffff;">
              Blog del equipo
            </h2>
            <p class="text-sm mb-6 leading-relaxed" style="color: oklch(72% 0.025 235);">
              Artículos del equipo. Sí, también escribimos.
            </p>
            <a
              href="/radar"
              class="inline-flex items-center gap-2 text-sm font-medium transition-opacity duration-150 hover:opacity-60 group"
              style="color: #fcbf49;"
            >
              Ver todos los artículos
              <span class="transition-transform duration-200 group-hover:translate-x-1" aria-hidden="true">→</span>
            </a>
          </div>

          <!-- 3 Radar article cards -->
          {[
            {
              categoria: 'Dev',
              accent: '#fcbf49',
              pos: 'first',
              title: 'Microsoft lanza 7 modelos MAI: GitHub Copilot ya no depende de OpenAI',
              date: 'Jun 2026',
              read: '4 min',
              href: '/radar/microsoft-mai-modelos-build-2026',
              image: radar1Img,
            },
            {
              categoria: 'IA',
              accent: '#fcbf49',
              pos: 'mid',
              title: 'Claude Opus 4.8: qué cambió y por qué importa si automatizas procesos',
              date: 'Jun 2026',
              read: '4 min',
              href: '/radar/claude-opus-4-8',
              image: radar2Img,
            },
            {
              categoria: 'IA',
              accent: '#fcbf49',
              pos: 'last',
              title: 'Claude Fable 5: Anthropic libera su primer modelo de clase Mythos',
              date: 'Jun 2026',
              read: '5 min',
              href: '/radar/claude-fable-5',
              image: radar3Img,
            },
          ].map((art) => (
            <a
              href={art.href}
              class={`conocimiento-card conocimiento-card--dark conocimiento-card--${art.pos} group`}
              style="background: oklch(10% 0.015 235 / 0.75); border: 1px solid oklch(28% 0.02 235 / 0.5);"
              data-reveal
            >
              <Image
                src={art.image}
                alt=""
                aria-hidden="true"
                widths={[400, 700]}
                sizes="(max-width: 1023px) 100vw, 25vw"
                loading="lazy"
                class="conocimiento-radar-image"
              />
              <span class="conocimiento-radar-shade" aria-hidden="true"></span>
              <div class="conocimiento-radar-content">
                <div class="flex items-center justify-between gap-3">
                  <span
                    class="text-xs font-mono tracking-wider uppercase block"
                    style={`color: ${art.accent}; font-family: var(--font-mono);`}
                  >
                    {art.categoria}
                  </span>
                  <span class="text-xs font-mono" style="color: rgba(255,255,255,0.62); font-family: var(--font-mono);">
                    {art.date} · {art.read}
                  </span>
                </div>
                <p class="conocimiento-radar-title" style="color: #ffffff;">
                  {art.title}
                </p>
                <span
                  class="conocimiento-arrow text-sm transition-transform duration-200 group-hover:translate-x-1"
                  style={`color: ${art.accent};`}
                  aria-hidden="true"
                >→</span>
              </div>
            </a>
          ))}

        </div>
      </div>
    </div>

  </section>
  </div><!-- /conocimiento-pin-stage -->


  <!-- ═══════════════════════════════════════════════════════════════════
       7. CTA FINAL, Full-bleed dark, impatto massimo
       ═══════════════════════════════════════════════════════════════════ -->
  <section
    id="cta"
    class="relative overflow-hidden flex items-center cta-overlap"
    style="background: var(--ink); min-height: 100dvh;"
    aria-label="Llamada a la acción"
  >
    <!-- Background image -->
    <Image
      src={ctaBgImg}
      alt=""
      aria-hidden="true"
      widths={[800, 1440, 1920]}
      sizes="100vw"
      class="absolute inset-0 w-full h-full pointer-events-none select-none"
      style="object-fit: cover; object-position: center; opacity: 0.18;"
      loading="lazy"
    />

    <!-- Glow -->
    <div
      class="absolute inset-0 pointer-events-none"
      style="background: radial-gradient(ellipse at center, var(--orange-glow) 0%, transparent 60%);"
      aria-hidden="true"
    ></div>

    <!-- A echo, bookend of the hero nucleus -->
    <div
      class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 pointer-events-none select-none"
      style="width: min(60vw, 760px); aspect-ratio: 10 / 11;"
      aria-hidden="true"
    >
      <svg viewBox="0 0 400 440" fill="none" class="w-full h-full" aria-hidden="true">
        <path d="M40 444 L200 36 L360 444" stroke="oklch(96% 0.005 60 / 0.05)" stroke-width="1.5" />
        <line x1="128" y1="312" x2="272" y2="312" stroke="oklch(96% 0.005 60 / 0.05)" stroke-width="1.5" />
        <circle cx="200" cy="36" r="4" fill="oklch(70% 0.19 55 / 0.4)" />
      </svg>
    </div>

    <div class="container-libera relative z-10 text-center" data-reveal>
      <h2
        class="display-hero mb-8 mx-auto max-w-5xl"
        style="font-size: clamp(2.5rem, 6vw, 5.5rem); color: var(--text-light);"
      >
        Tu negocio ya funciona.
        <span style="color: var(--orange);"> Imagínalo funcionando en orden.</span>
      </h2>

      <p class="text-lg mb-12 max-w-xl mx-auto" style="color: var(--text-light);">
        Empecemos por un diagnóstico: revisamos cómo te encuentran, cómo respondes
        y qué parte de tu sistema digital necesita orden primero. Sin costo y sin compromiso.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href="/diagnostico" class="btn btn-primary text-lg">
          Solicitar diagnóstico
          <span class="btn-icon" aria-hidden="true">
            <svg width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5">
              <path d="M1 11L11 1M11 1H2M11 1v9"/>
            </svg>
          </span>
        </a>
        <a href="/contacto" class="btn btn-ghost-dark text-lg">
          Hablar con el equipo
        </a>
      </div>
    </div>
  </section>

</Layout>

<style>
  @keyframes scrollDrop {
    0%   { transform: translateY(-100%); }
    100% { transform: translateY(200%); }
  }

  .hero-pin-stage {
    position: relative;
    min-height: 200dvh;
    background: var(--ink);
    z-index: 0;
  }

  .hero-panel {
    position: sticky;
    top: 0;
    z-index: 0;
  }

  .problem-pin-stage {
    position: relative;
    min-height: 280dvh;
    margin-top: -100dvh;
    background: var(--surface);
    z-index: 2;
  }

  .problem-overlap {
    position: sticky;
    top: 0;
    height: 100dvh;
    overflow: hidden;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .problem-left {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: clamp(2rem, 4dvh, 3.5rem);
    padding-top: calc(var(--nav-height, 4rem) + 3rem);
    padding-bottom: clamp(1.5rem, 3dvh, 2.5rem);
    padding-right: clamp(1.25rem, 4vw, 3.5rem);
    padding-left: max(clamp(1.25rem, 4vw, 3.5rem), calc((100vw - 1440px) / 2 + clamp(1.25rem, 4vw, 3.5rem)));
    overflow: hidden;
  }

  .problem-content {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    padding-top: 1.5rem;
  }

  .problem-title {
    font-family: var(--font-display);
    font-size: clamp(1.4rem, 2.6vw, 2.6rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }

  .problem-body {
    font-size: clamp(1rem, 1.1vw, 1.1rem);
    line-height: 1.65;
    max-width: 100%;
    text-wrap: pretty;
  }

  .problem-right {
    height: 100dvh;
    overflow: hidden;
    padding-top: calc(var(--nav-height, 4rem) + 3rem);
    box-sizing: border-box;
  }

  .problem-mobile-visual {
    display: none;
  }

  .problem-img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center center;
    display: block;
  }

  .problem-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0;
    border-top: 1px solid var(--border);
    flex-shrink: 0;
  }

  .problem-stat {
    padding: clamp(0.75rem, 1.5dvh, 1.25rem) 0;
    border-right: 1px solid var(--border);
    padding-right: clamp(0.75rem, 2vw, 1.5rem);
  }

  .problem-stat:nth-child(2) {
    border-right: none;
    padding-left: clamp(0.75rem, 2vw, 1.5rem);
    padding-right: 0;
  }

  .problem-stat:nth-child(3),
  .problem-stat:nth-child(4) {
    border-top: 1px solid var(--border);
  }

  .problem-stat:nth-child(4) {
    border-right: none;
    padding-left: clamp(0.75rem, 2vw, 1.5rem);
    padding-right: 0;
  }

  .problem-stat:nth-child(3) {
    padding-right: clamp(0.75rem, 2vw, 1.5rem);
  }

  .problem-stat-value {
    font-size: clamp(1.25rem, 2vw, 2rem);
    font-weight: 900;
    font-style: italic;
    color: var(--orange);
    line-height: 1;
    margin-bottom: 0.35rem;
    font-variant-numeric: tabular-nums;
  }

  .problem-stat-text {
    font-size: clamp(0.75rem, 0.85vw, 0.82rem);
    line-height: 1.5;
    color: var(--text-secondary);
    margin-bottom: 0.25rem;
  }

  .problem-stat-source {
    font-size: 0.75rem;
    color: var(--text-tertiary, oklch(60% 0.01 60));
    font-family: var(--font-mono);
    letter-spacing: 0.02em;
  }

  /* Tablet + Mobile — reset tutti i pin-stage e sticky scroll */
  @media (max-width: 1023px) {
    /* ── Hero ── */
    .hero-pin-stage {
      min-height: auto;
    }

    .hero-panel {
      position: relative;
    }

    /* ── Problema ── */
    .problem-pin-stage {
      min-height: auto;
      margin-top: 0;
    }

    .problem-overlap {
      height: auto;
      min-height: 100dvh;
      position: relative;
      grid-template-columns: 1fr;
    }

    .problem-left {
      padding: calc(var(--nav-height, 4rem) + 2rem) clamp(1.25rem, 4vw, 3.5rem) 2rem;
    }

    .problem-right {
      height: 45dvh;
      padding-top: 0;
    }

    .problem-title {
      font-size: clamp(1.6rem, 4vw, 2.4rem);
    }

    .problem-body {
      font-size: 1rem;
    }

    /* ── Solutions ── */
    .solutions-overlap {
      margin-top: 0;
    }

    .solutions-sticky {
      padding-top: 0;
    }

    /* ── Trabajo ── */
    .work-pin-stage {
      min-height: auto;
      margin-top: 0;
    }

    .work-overlap {
      position: relative;
      top: auto;
      height: auto;
      overflow: visible;
      padding-top: calc(var(--nav-height, 72px) + 2rem);
      padding-bottom: 3rem;
    }

    .work-inner {
      height: auto;
    }

    .work-card-grid {
      height: auto;
      grid-template-rows: auto;
      flex: none;
    }

    .work-card {
      min-height: clamp(200px, 40vw, 320px);
    }

    /* ── Conocimiento ── */
    .conocimiento-pin-stage {
      min-height: auto;
      height: auto;
      margin-top: 0;
    }

    #conocimiento {
      position: relative;
      top: auto;
      height: auto;
      grid-template-rows: auto;
    }

    .conocimiento-half {
      padding-block: 4rem 3rem;
    }

    .conocimiento-half > .container-libera {
      height: auto;
      align-items: flex-start;
    }

    .conocimiento-half-grid {
      grid-template-columns: 1fr;
      height: auto;
    }

    .conocimiento-card {
      aspect-ratio: auto;
      min-height: 180px;
    }

    /* ── CTA ── */
    .cta-overlap {
      margin-top: 0;
    }
  }

  /* Mobile */
  @media (max-width: 640px) {
    .problem-overlap {
      min-height: auto;
    }

    .work-card {
      min-height: 260px;
    }

    .work-card-grid {
      grid-template-columns: 1fr;
    }

    .conocimiento-card {
      min-height: auto;
    }

    .problem-left {
      padding: calc(var(--nav-height, 4rem) + 1.5rem) 1.25rem 1.5rem;
      gap: 2rem;
    }

    .problem-right {
      height: 40dvh;
    }

    .problem-stats {
      grid-template-columns: 1fr;
    }

    .problem-stat {
      border-right: none;
      padding-right: 0;
      padding-left: 0;
    }

    .problem-stat:nth-child(2) {
      border-top: 1px solid var(--border);
      padding-left: 0;
    }

    .problem-stat:nth-child(4) {
      padding-left: 0;
    }
  }

  .solutions-overlap {
    position: relative;
    z-index: 3;
    margin-top: -100dvh;
    min-height: 560dvh;
    padding-block: 0;
  }

  .method-overlap {
    position: relative;
    z-index: 4;
    margin-top: 0;
  }

  .work-pin-stage {
    min-height: 250dvh;
    position: relative;
    z-index: 5;
    margin-top: -82dvh;
  }

  .work-overlap {
    position: sticky;
    top: 0;
    height: 100dvh;
    overflow: hidden;
    z-index: 5;
    display: flex;
    align-items: flex-start;
    padding-top: calc(var(--nav-height, 72px) + 1.5rem);
    padding-bottom: 1.5rem;
  }

  .work-inner {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .work-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .work-ver-todo {
    white-space: nowrap;
    flex-shrink: 0;
    padding-bottom: 0.15rem;
  }

  .work-card-grid {
    flex: 1;
    min-height: 0;
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
  }

  .work-card {
    min-height: 0;
  }

  /* ── Conocimiento pin + CTA overlap ───────────────────────── */
  .conocimiento-pin-stage {
    min-height: 250dvh;
    position: relative;
    z-index: 6;
    margin-top: -100dvh;
  }

  #conocimiento {
    position: sticky;
    top: 0;
    height: 100dvh;
    overflow: hidden;
    display: grid;
    grid-template-rows: 1fr;
  }

  .conocimiento-half {
    display: flex;
    align-items: center;
    overflow: hidden;
    padding-block: calc(var(--nav-height, 72px) + 1.5rem) 1.5rem;
  }

  .conocimiento-half > .container-libera {
    display: flex;
    align-items: stretch;
  }

  .conocimiento-half-grid {
    display: grid;
    grid-template-columns: minmax(200px, 240px) repeat(3, 1fr);
    gap: 0;
    align-items: stretch;
    width: 100%;
    min-width: 0;
  }

  .conocimiento-card--first { border-radius: 0; }
  .conocimiento-card--mid   { border-radius: 0; }
  .conocimiento-card--last  { border-radius: 0; }

  .conocimiento-card {
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
    aspect-ratio: 1 / 1;
    position: relative;
    overflow: hidden;
    isolation: isolate;
    transition: opacity 0.25s ease, transform 0.25s cubic-bezier(0.16, 1, 0.3, 1);
    min-width: 0;
  }
  .conocimiento-card:hover {
    opacity: 0.88;
    transform: translateY(-2px);
  }

  .conocimiento-guide-image {
    position: absolute;
    inset: 0;
    z-index: -2;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.01);
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .conocimiento-card:hover .conocimiento-guide-image {
    transform: scale(1.045);
  }

  .conocimiento-card-content {
    position: relative;
    z-index: 1;
  }

  .conocimiento-card:not(.conocimiento-card--dark) {
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .conocimiento-guide-title {
    font-size: clamp(1.2rem, 1.7vw, 1.5rem);
  }

  .conocimiento-arrow {
    position: absolute;
    bottom: 1.5rem;
    right: 1.5rem;
    z-index: 2;
  }

  .conocimiento-card--dark {
    justify-content: flex-end;
    aspect-ratio: auto;
    min-height: clamp(360px, 56vh, 590px);
  }

  .conocimiento-radar-image {
    position: absolute;
    inset: 0;
    z-index: -2;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: scale(1.01);
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .conocimiento-radar-shade {
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      linear-gradient(180deg, oklch(6% 0.012 235 / 0.05) 0%, oklch(6% 0.012 235 / 0.82) 100%),
      linear-gradient(0deg, oklch(6% 0.012 235 / 0.97) 0%, oklch(6% 0.012 235 / 0.75) 40%, transparent 60%);
  }

  .conocimiento-card--dark:hover .conocimiento-radar-image {
    transform: scale(1.045);
  }

  .conocimiento-radar-content {
    position: absolute;
    top: 60%;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    min-width: 0;
    padding: 1.25rem 1.75rem 1.75rem;
  }

  .conocimiento-radar-content > div {
    min-width: 0;
    flex-wrap: wrap;
  }

  .conocimiento-radar-title {
    font-size: clamp(0.95rem, 1.1vw, 1.05rem);
    font-weight: 600;
    line-height: 1.35;
  }

  .conocimiento-arrow {
    margin-top: 0.25rem;
  }

  .cta-overlap {
    position: relative;
    z-index: 7;
    margin-top: calc(var(--nav-height, 72px) - 100dvh);
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-pin-stage {
      min-height: auto;
    }

    .hero-panel {
      position: relative;
    }

    .problem-pin-stage {
      min-height: auto;
      margin-top: 0;
    }

    .problem-overlap {
      position: relative;
      min-height: auto;
    }

    .solutions-overlap {
      margin-top: 0;
    }

    .method-overlap {
      margin-top: 0;
    }

    .work-overlap {
      margin-top: 0;
    }

    .conocimiento-pin-stage {
      min-height: auto;
      margin-top: 0;
    }

    #conocimiento {
      position: relative;
      top: auto;
      height: auto;
    }
  }

  /* Trabajo card: overlay appare all'hover */
  #trabajo a:hover [data-card-overlay] {
    opacity: 1;
  }


  .solutions-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0;
    align-items: start;
    position: relative;
    min-height: calc(100dvh - var(--nav-height));
  }

  .solutions-image-col {
    position: sticky;
    top: var(--nav-height);
    height: calc(100dvh - var(--nav-height));
    overflow: hidden;
    padding-top: calc(var(--nav-height, 4rem) * 0.75);
    padding-bottom: 2rem;
  }

  .solutions-pin {
    position: sticky;
    top: var(--nav-height);
    min-height: calc(100dvh - var(--nav-height));
    display: flex;
    align-items: flex-start;
  }

  .solutions-sticky {
    position: relative;
    top: auto;
    min-height: auto;
    padding-top: 2rem;
    padding-left: clamp(1.5rem, 4vw, 3rem);
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    text-align: right;
  }

  .solutions-intro {
    max-width: 100%;
    text-align: right;
  }

  .solutions-intro .section-label {
    margin-bottom: 1.25rem;
  }

  .solutions-title {
    line-height: 1.08;
    text-wrap: balance;
  }

  .solution-visual-shell {
    width: 100%;
    height: 100%;
    margin-top: 0;
  }

  .solution-visual-frame {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: hidden;
    border-radius: 1.25rem;
    background: var(--surface);
    border: none;
    box-shadow: none;
  }

  .solution-visual-frame::after {
    display: none;
  }

  .solution-visual-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    opacity: var(--solution-image-opacity, 0);
    transform: scale(var(--solution-image-scale, 0.985));
    transition:
      opacity 0.44s var(--ease-out-expo),
      filter 0.44s var(--ease-out-expo),
      transform 0.44s var(--ease-out-expo);
    filter: saturate(0.84) brightness(0.9);
  }

  .solution-visual-image.is-active {
    --solution-image-opacity: 1;
    --solution-image-scale: 1;
    filter: saturate(1) brightness(1);
  }

  .solution-visual-caption {
    position: relative;
    min-height: 1.4rem;
    margin-top: 1rem;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--text-secondary);
  }

  .solution-visual-label {
    position: absolute;
    inset: 0 auto auto 0;
    opacity: var(--solution-label-opacity, 0);
    transition: opacity 0.3s var(--ease-premium);
  }

  .solution-visual-label.is-active {
    --solution-label-opacity: 1;
  }

  .solutions-card-rail {
    position: relative;
    min-height: min(560px, calc(100dvh - var(--nav-height) - 3rem));
    padding-top: 0;
  }

  .solution-card {
    --solution-accent: var(--orange);
    --solution-card-opacity: 0;
    position: absolute;
    inset: 50% 0 auto 0;
    display: flex;
    min-height: auto;
    flex-direction: column;
    justify-content: space-between;
    padding: 0;
    background: transparent;
    border-top: 0;
    color: var(--text-primary);
    opacity: var(--solution-card-opacity);
    transform: translate3d(0, -50%, 0);
    transition:
      opacity 0.3s var(--ease-out-expo),
      color 0.3s var(--ease-premium);
    will-change: opacity;
    pointer-events: none;
  }

  .solution-card:first-child {
    border-top: 0;
  }

  .solution-card.is-active {
    pointer-events: auto;
  }

  .solution-card-glow {
    display: none;
  }

  .solution-card-mobile-visual {
    display: none;
  }

  .solution-card.is-active .solution-card-glow,
  .solution-card:hover .solution-card-glow {
    opacity: 0;
  }

  .solution-card-index {
    position: absolute;
    right: 0;
    top: clamp(1.75rem, 5dvh, 4rem);
    font-family: var(--font-display);
    font-size: clamp(4.5rem, 8vw, 7.5rem);
    font-weight: 800;
    line-height: 1;
    color: var(--solution-accent);
    opacity: 0.055;
    pointer-events: none;
    user-select: none;
  }

  .solution-card-title {
    position: relative;
    max-width: min(100%, 45rem);
    text-align: right;
    margin-bottom: 1.25rem;
    font-family: var(--font-display);
    font-size: clamp(1.1rem, 1.5vw, 1.4rem);
    font-weight: 650;
    letter-spacing: -0.01em;
    line-height: 1.2;
    color: #003049;
  }

  .solution-card-copy {
    position: relative;
    max-width: 45rem;
    text-align: right;
    margin-bottom: 1.5rem;
    font-size: clamp(1rem, 1.2vw, 1.15rem);
    line-height: 1.55;
    color: var(--text-secondary);
  }

  .solution-service-tag {
    position: relative;
    display: inline-flex;
    align-items: center;
    min-height: 2rem;
    padding: 0.15rem 0;
    background: transparent;
    border: 0;
    color: var(--text-secondary);
    font-size: 0.8125rem;
  }

  .solution-service-tag::before {
    content: '';
    width: 0.35rem;
    height: 0.35rem;
    margin-right: 0.55rem;
    border-radius: 9999px;
    background: var(--solution-accent);
    opacity: 0.7;
  }

  .solution-card-arrow {
    display: inline-flex;
    transform: translate3d(0, 0, 0);
    transition: transform 0.25s var(--ease-premium);
  }

  .solution-card:hover .solution-card-arrow {
    transform: translate3d(0.25rem, -0.0625rem, 0);
  }

  @media (max-width: 1023px) {
    .solutions-overlap {
      min-height: auto;
      padding-block: 6rem;
    }

    .solutions-pin {
      position: relative;
      top: auto;
      min-height: auto;
      display: block;
    }

    .solutions-layout {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      position: relative;
      top: auto;
      min-height: auto;
    }

    .solutions-image-col {
      position: relative;
      top: auto;
      height: 50dvh;
      padding-top: 0;
      padding-bottom: 0;
    }

    .solutions-sticky {
      position: relative;
      top: auto;
      min-height: auto;
      padding-top: 0;
    }

    .solution-visual-shell {
      width: 100%;
      height: 100%;
      margin-top: 0;
    }

    .solutions-card-rail {
      display: grid;
      gap: 1rem;
      min-height: auto;
      padding-top: 0;
    }

    .solution-card {
      position: relative;
      inset: auto;
      min-height: auto;
      opacity: 1;
      transform: none;
      padding: clamp(2rem, 7vw, 3.25rem) 0;
      pointer-events: auto;
    }
  }

  @media (max-width: 640px) {
    .solution-card {
      min-height: auto;
      padding: 1.5rem 0;
    }

    .solution-card-title {
      max-width: min(100%, 39rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .solution-card,
    .solution-visual-image,
    .solution-visual-label,
    .solution-card-arrow,
    .solution-card-glow {
      transform: none !important;
      transition-duration: 0.01ms !important;
    }

    .solution-card {
      opacity: 1;
      transform: none;
      will-change: auto;
    }
  }

  /* ── Hero A, slow drift + apex node pulse ───────────────────────── */
  .hero-a {
    transform: translateY(-50%);
    animation: heroDrift 14s var(--ease-smooth) infinite alternate;
    will-change: transform;
  }

  @keyframes heroDrift {
    from { transform: translateY(calc(-50% - 8px)); }
    to   { transform: translateY(calc(-50% + 8px)); }
  }

  .hero-a-node {
    animation: nodePulse 3.2s var(--ease-smooth) infinite;
    transform-origin: center;
    transform-box: fill-box;
  }

  @keyframes nodePulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%      { opacity: 0.55; transform: scale(1.35); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-a,
    .hero-a-node {
      animation: none;
    }
  }

  .method-scroll {
    min-height: 1240dvh;
    margin-bottom: 0;
    position: relative;
  }

  .method-sticky {
    min-height: 100dvh;
    position: sticky;
    top: 0;
    display: flex;
    align-items: stretch;
    padding-block: calc(var(--nav-height) + 1.25rem) 2rem;
  }

  .method-layout {
    min-height: calc(100dvh - var(--nav-height) - 3.25rem);
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: clamp(1.25rem, 3dvh, 2.75rem);
    position: relative;
    z-index: 1;
  }

  .method-copy {
    max-width: min(100%, 980px);
  }

  .method-title {
    --method-title-fit: 1;
    --method-title-opacity: 1;
    /* Restores the original all-caps look this section was tuned around
       (the global h1/h2 uppercase rule was removed for long headlines) */
    text-transform: uppercase;
    color: var(--text-light);
    font-size: clamp(1.05rem, calc(4.6vw * var(--method-title-fit)), 4.65rem);
    line-height: 1.12;
    max-width: 100%;
    opacity: var(--method-title-opacity);
    text-align: left;
    transform: none;
    will-change: opacity;
  }

  .method-title-line {
    --method-line-opacity: 0;
    --method-line-x: 0px;
    --method-line-y: 0px;
    --method-line-scale: 1;
    --method-line-accent: 0%;
    display: block;
    width: max-content;
    max-width: none;
    color: var(--text-light);
    opacity: var(--method-line-opacity);
    transform: translate3d(var(--method-line-x), var(--method-line-y), 0) scale(var(--method-line-scale));
    transform-origin: left top;
    white-space: nowrap;
    transition: none;
    will-change: transform, opacity;
  }

  .method-title-action {
    color: color-mix(in oklch, var(--orange) var(--method-line-accent), var(--text-light));
  }

  .method-stage {
    min-height: clamp(390px, 48dvh, 560px);
    position: relative;
    overflow: hidden;
  }

  .method-step {
    --method-step-x: 0vw;
    --method-step-y: 5.5rem;
    --method-step-opacity: 0;
    --method-step-focus: 0%;
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-end;
    padding-block: clamp(0.5rem, 2vw, 1.5rem);
    padding-inline: 0;
    opacity: var(--method-step-opacity);
    transform: translate3d(var(--method-step-x), var(--method-step-y), 0);
    transition:
      opacity 0.42s var(--ease-out-expo),
      transform 0.42s var(--ease-out-expo);
    will-change: transform, opacity;
    pointer-events: none;
  }

  .method-step h3 {
    color: var(--text-light);
    font-size: calc(clamp(4.5rem, 14vw, 15rem) * var(--method-step-title-fit, 1));
    font-style: normal;
    font-weight: 900;
    line-height: 0.9;
    text-align: right;
    text-transform: uppercase;
    letter-spacing: 0.015em;
    max-width: 100%;
    white-space: nowrap;
  }

  .method-step p {
    color: oklch(86% 0.012 60);
    font-size: clamp(1.35rem, 2.4vw, 2rem);
    line-height: 1.35;
    max-width: 58rem;
    text-align: right;
  }

  @media (max-width: 1023px) {
    .method-sticky {
      padding-block: calc(var(--nav-height) + 1rem) 1.25rem;
    }

    .method-layout {
      min-height: calc(100dvh - var(--nav-height) - 2.25rem);
      gap: clamp(1rem, 2.5dvh, 2rem);
    }

    .method-title {
      font-size: clamp(0.95rem, 4.65vw, 2rem);
    }

    .method-stage {
      min-height: 420px;
    }

    .method-step {
      padding-inline: 0;
    }

    .method-step h3 {
      font-size: calc(clamp(3.4rem, 16vw, 8.75rem) * var(--method-step-title-fit, 1));
    }

    .method-step p {
      font-size: clamp(1.05rem, 3vw, 1.45rem);
      max-width: min(100%, 42rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .method-scroll {
      min-height: auto;
      padding-block: 7rem;
    }

    .method-sticky {
      position: relative;
      min-height: auto;
      padding-block: 0;
    }

    .method-layout {
      min-height: auto;
    }

    .method-title {
      --method-title-opacity: 1;
    }

    .method-title-line {
      --method-line-opacity: 1;
      --method-line-x: 0px;
      --method-line-y: 0px;
      --method-line-scale: 0.66;
      --method-line-accent: 100%;
    }

    .method-stage {
      min-height: auto;
      overflow: visible;
      display: grid;
      gap: 1px;
      background: var(--border-dark);
      border-top: 0;
    }

    .method-step {
      --method-step-focus: 100%;
      position: relative;
      opacity: 1;
      transform: none;
      background: var(--ink-mid);
    }
  }

  /* Metodo prototype clone: same idea, tighter scroll rhythm. */
  .method-clone-scroll {
    min-height: 900dvh;
    margin-bottom: 0;
    position: relative;
  }

  .method-clone-sticky {
    min-height: 100dvh;
    position: sticky;
    top: 0;
    display: flex;
    align-items: stretch;
    padding-block: calc(var(--nav-height) + 1.25rem) 2rem;
  }

  .method-clone-layout {
    min-height: calc(100dvh - var(--nav-height) - 3.25rem);
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: clamp(1.25rem, 3dvh, 2.75rem);
    position: relative;
    z-index: 1;
  }

  .method-clone-copy {
    max-width: min(100%, 980px);
  }

  .method-clone-title {
    --method-clone-title-fit: 1;
    --method-clone-title-opacity: 1;
    text-transform: uppercase;
    color: var(--text-light);
    font-size: clamp(1.05rem, calc(4.6vw * var(--method-clone-title-fit)), 4.65rem);
    line-height: 1.12;
    max-width: 100%;
    opacity: var(--method-clone-title-opacity);
    text-align: left;
    transform: none;
    will-change: opacity;
  }

  .method-clone-title-line {
    --method-clone-line-opacity: 0;
    --method-clone-line-x: 0px;
    --method-clone-line-y: 0px;
    --method-clone-line-scale: 1;
    --method-clone-line-accent: 0%;
    display: block;
    width: max-content;
    max-width: none;
    color: var(--text-light);
    opacity: var(--method-clone-line-opacity);
    transform: translate3d(var(--method-clone-line-x), var(--method-clone-line-y), 0) scale(var(--method-clone-line-scale));
    transform-origin: left top;
    white-space: nowrap;
    transition: none;
    will-change: transform, opacity;
  }

  .method-clone-title-action {
    color: color-mix(in oklch, var(--orange) var(--method-clone-line-accent), var(--text-light));
  }

  .method-clone-stage {
    min-height: clamp(390px, 48dvh, 560px);
    position: relative;
    overflow: hidden;
  }

  .method-clone-step {
    --method-clone-step-x: 0vw;
    --method-clone-step-y: 5.5rem;
    --method-clone-step-opacity: 0;
    --method-clone-step-focus: 0%;
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-end;
    padding-block: clamp(0.5rem, 2vw, 1.5rem);
    padding-inline: 0;
    opacity: var(--method-clone-step-opacity);
    transform: translate3d(var(--method-clone-step-x), var(--method-clone-step-y), 0);
    will-change: transform, opacity;
    pointer-events: none;
  }

  .method-clone-step h3 {
    color: var(--text-light);
    font-size: calc(clamp(4.5rem, 14vw, 15rem) * var(--method-clone-step-title-fit, 1));
    font-style: normal;
    font-weight: 900;
    line-height: 0.9;
    text-align: right;
    text-transform: uppercase;
    letter-spacing: 0.015em;
    max-width: 100%;
    white-space: nowrap;
  }

  .method-clone-step p {
    color: oklch(86% 0.012 60);
    font-size: clamp(1.35rem, 2.4vw, 2rem);
    line-height: 1.35;
    max-width: 58rem;
    text-align: right;
  }

  @media (max-width: 1023px) {
    .method-clone-sticky {
      padding-block: calc(var(--nav-height) + 1rem) 1.25rem;
    }

    .method-clone-layout {
      min-height: calc(100dvh - var(--nav-height) - 2.25rem);
      gap: clamp(1rem, 2.5dvh, 2rem);
    }

    .method-clone-title {
      font-size: clamp(0.95rem, 4.65vw, 2rem);
    }

    .method-clone-stage {
      min-height: 420px;
    }

    .method-clone-step h3 {
      font-size: calc(clamp(3.4rem, 16vw, 8.75rem) * var(--method-clone-step-title-fit, 1));
    }

    .method-clone-step p {
      font-size: clamp(1.05rem, 3vw, 1.45rem);
      max-width: min(100%, 42rem);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .method-clone-scroll {
      min-height: auto;
      padding-block: 7rem;
    }

    .method-clone-sticky {
      position: relative;
      min-height: auto;
      padding-block: 0;
    }

    .method-clone-layout {
      min-height: auto;
    }

    .method-clone-title {
      --method-clone-title-opacity: 1;
    }

    .method-clone-title-line {
      --method-clone-line-opacity: 1;
      --method-clone-line-x: 0px;
      --method-clone-line-y: 0px;
      --method-clone-line-scale: 0.66;
      --method-clone-line-accent: 100%;
    }

    .method-clone-stage {
      min-height: auto;
      overflow: visible;
      display: grid;
      gap: 1px;
      background: var(--border-dark);
    }

    .method-clone-step {
      --method-clone-step-focus: 100%;
      position: relative;
      opacity: 1;
      transform: none;
      background: var(--ink-mid);
    }
  }

  @media (max-width: 900px) {
    .conocimiento-pin-stage {
      min-height: auto;
      height: auto;
      margin-top: 0;
    }

    #conocimiento {
      position: relative;
      top: auto;
      height: auto;
      overflow: visible;
      grid-template-rows: auto;
    }

    .conocimiento-half {
      padding-block: 5rem;
    }

    .conocimiento-half > .container-libera {
      height: auto;
    }

    .conocimiento-half-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
      height: auto;
    }

    .conocimiento-half-grid > div:first-child {
      padding-block: 0 1.5rem !important;
    }

    .conocimiento-card--dark {
      min-height: 320px;
      aspect-ratio: 16 / 10;
    }

    .cta-overlap {
      margin-top: 0;
    }
  }

  @media (max-width: 520px) {
    .conocimiento-card--dark {
      min-height: 280px;
      aspect-ratio: auto;
    }
  }

  /* Mobile source-of-truth: no desktop overlap or detached solution imagery. */
  @media (max-width: 1023px) {
    .problem-pin-stage {
      min-height: auto;
      margin-top: 0;
    }

    .problem-overlap {
      position: relative;
      top: auto;
      height: auto;
      min-height: auto;
      overflow: visible;
    }

    .problem-left {
      overflow: visible;
    }

    .problem-mobile-visual {
      display: flex;
      justify-content: center;
      width: 100%;
      margin: 2.5rem 0;
    }

    .problem-mobile-visual img {
      display: block;
      width: min(100%, 34rem);
      height: auto;
      object-fit: contain;
    }

    .problem-right {
      display: none;
    }

    .solutions-overlap {
      margin-top: 0;
      min-height: auto;
      padding-block: 5rem;
    }

    .solutions-image-col {
      display: none;
    }

    .solutions-layout {
      gap: 0;
    }

    .solutions-intro {
      margin-bottom: 2.5rem;
    }

    .solutions-card-rail {
      gap: 3.5rem;
    }

    .solution-card {
      display: block;
      padding: 0 0 3.5rem;
      border-bottom: 1px solid var(--border);
    }

    .solution-card:last-child {
      padding-bottom: 0;
      border-bottom: 0;
    }

    .solution-card-mobile-visual {
      display: block;
      width: 70%;
      aspect-ratio: 4 / 3;
      margin: 0 auto 1.75rem;
      overflow: hidden;
      background: #ffffff;
    }

    .solution-card-mobile-visual img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      display: block;
    }

    .solution-card .flex.items-center.justify-end {
      justify-content: flex-start;
      margin-bottom: 1.25rem;
    }

    .solution-card-title,
    .solution-card-copy {
      max-width: 42rem;
      text-align: left;
    }

    .solution-card .flex.flex-wrap.justify-end {
      justify-content: flex-start;
    }

    .solution-card > .relative.mt-10 {
      justify-content: flex-start;
      margin-top: 1.75rem;
    }

    .solution-card-index {
      top: auto;
      right: 0;
      bottom: 3.5rem;
    }
  }

  @media (max-width: 640px) {
    .problem-left {
      padding-bottom: 2.5rem;
    }

    .solutions-overlap {
      padding-block: 4rem;
    }

    .solutions-title {
      text-align: left;
    }

    .solution-card-mobile-visual {
      margin-bottom: 1.5rem;
    }
  }
</style>

<script>
  // Fit hero headline to exact container width — binary search on font-size
  function fitHeroHeadline() {
    const h1 = document.querySelector<HTMLElement>('[data-hero-headline]');
    if (!h1) return;
    const targetWidth = h1.clientWidth;
    const lines = [...h1.querySelectorAll<HTMLElement>('[data-hero-line]')];
    let lo = 10, hi = 90;
    while (hi - lo > 0.25) {
      const mid = (lo + hi) / 2;
      h1.style.fontSize = mid + 'px';
      const maxWidth = Math.max(...lines.map(l => l.scrollWidth));
      if (maxWidth <= targetWidth) lo = mid; else hi = mid;
    }
    h1.style.fontSize = lo + 'px';
  }
  fitHeroHeadline();
  window.addEventListener('resize', fitHeroHeadline);
</script>

<script>
  const solutionCards = [...document.querySelectorAll<HTMLElement>('[data-solution-card]')];
  const solutionImages = [...document.querySelectorAll<HTMLElement>('[data-solution-image]')];
  const solutionLabels = [...document.querySelectorAll<HTMLElement>('[data-solution-label]')];
  const solutionsSection = document.querySelector<HTMLElement>('[data-solutions-section]');

  const setActiveSolution = (id: string, intensity = 1) => {
    solutionCards.forEach((card) => {
      card.classList.toggle('is-active', card.dataset.solutionCard === id);
    });

    solutionImages.forEach((image) => {
      const isActive = image.dataset.solutionImage === id;
      image.classList.toggle('is-active', isActive);
      image.style.setProperty('--solution-image-opacity', isActive ? intensity.toFixed(3) : '0');
      image.style.setProperty('--solution-image-scale', isActive ? (0.985 + intensity * 0.015).toFixed(3) : '0.985');
    });

    solutionLabels.forEach((label) => {
      const isActive = label.dataset.solutionLabel === id;
      label.classList.toggle('is-active', isActive);
      label.style.setProperty('--solution-label-opacity', isActive ? intensity.toFixed(3) : '0');
    });
  };

  if (solutionsSection && solutionCards.length > 0) {
    const reduceSolutionsMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const ease = (value: number) => value * value * (3 - 2 * value);
    const clamp = (value: number, min = 0, max = 1) => Math.min(max, Math.max(min, value));
    let solutionTicking = false;

    const updateSolutions = () => {
      solutionTicking = false;

      if (window.innerWidth < 1024 || reduceSolutionsMotion) {
        solutionCards.forEach((card) => {
          card.classList.add('is-active');
          card.style.removeProperty('--solution-card-opacity');
        });
        setActiveSolution(solutionCards[0]?.dataset.solutionCard ?? '', 1);
        return;
      }

      const scrollable = Math.max(1, solutionsSection.offsetHeight - window.innerHeight);
      const rawProgress = clamp((window.scrollY - solutionsSection.offsetTop) / scrollable);
      const startPause = 0.04;
      const endPause = 0.04;
      const progress = clamp((rawProgress - startPause) / (1 - startPause - endPause));
      const maxIndex = Math.max(1, solutionCards.length - 1);
      const revealWindow = 0.085;
      let activeId = solutionCards[0]?.dataset.solutionCard ?? '';
      let activeIntensity = 0;
      let activeVisualOpacity = 0;

      solutionCards.forEach((card, index) => {
        const center = index / maxIndex;
        const distance = Math.abs(progress - center);
        const intensity = ease(clamp(1 - distance / revealWindow));
        const textOpacity = progress <= center
          ? (distance <= revealWindow ? 1 : 0)
          : ease(clamp(1 - (progress - center) / revealWindow));

        card.style.setProperty('--solution-card-opacity', textOpacity.toFixed(3));
        card.classList.toggle('is-active', textOpacity > 0.56);
        card.setAttribute('aria-hidden', textOpacity > 0.18 ? 'false' : 'true');

        if (intensity > activeIntensity) {
          activeIntensity = intensity;
          activeVisualOpacity = textOpacity;
          activeId = card.dataset.solutionCard ?? activeId;
        }
      });

      setActiveSolution(activeId, activeVisualOpacity);
    };

    const requestSolutionsUpdate = () => {
      if (solutionTicking) return;
      solutionTicking = true;
      window.requestAnimationFrame(updateSolutions);
    };

    updateSolutions();
    window.addEventListener('scroll', requestSolutionsUpdate, { passive: true });
    window.addEventListener('resize', requestSolutionsUpdate, { passive: true });
  }

  {
    const methodSection = document.querySelector('[data-method-clone-section]');
    const methodFrame = document.querySelector('.method-clone-sticky');
    const methodSteps = [...document.querySelectorAll('[data-method-clone-step]')];
    const methodTitle = document.querySelector('[data-method-clone-title]');
    const methodTitleLines = [...document.querySelectorAll('[data-method-clone-title-line]')];
    const methodStepTitles = [...document.querySelectorAll('[data-method-clone-step-title]')];
    const reduceMethodMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    let sectionTop = 0;
    let sectionHeight = 0;
    const cacheSectionRect = () => {
      if (!methodSection) return;
      sectionTop = methodSection.offsetTop;
      sectionHeight = methodSection.offsetHeight;
    };

    const fitMethodTitle = () => {
      if (!methodTitle) return;
      methodTitle.style.setProperty('--method-clone-title-fit', '1');
      let fit = 1;

      while (methodTitle.scrollWidth > methodTitle.clientWidth && fit > 0.52) {
        fit -= 0.02;
        methodTitle.style.setProperty('--method-clone-title-fit', fit.toFixed(2));
      }
    };

    const fitMethodStepTitles = () => {
      if (methodStepTitles.length === 0) return;

      document.documentElement.style.setProperty('--method-clone-step-title-fit', '1');
      let fit = 1;

      while (
        methodStepTitles.some((title) => title.scrollWidth > title.clientWidth) &&
        fit > 0.5
      ) {
        fit -= 0.02;
        document.documentElement.style.setProperty('--method-clone-step-title-fit', fit.toFixed(2));
      }
    };

    const updateMethodSteps = () => {
      if (!methodSection || methodSteps.length === 0 || reduceMethodMotion) return;

      const hideMethodSequence = () => {
        methodTitle?.style.setProperty('--method-clone-title-opacity', '0');

        methodTitleLines.forEach((line) => {
          line.style.setProperty('--method-clone-line-opacity', '0');
          line.style.setProperty('--method-clone-line-x', '0px');
          line.style.setProperty('--method-clone-line-y', '0px');
          line.style.setProperty('--method-clone-line-scale', '1');
          line.style.setProperty('--method-clone-line-accent', '0%');
        });

        methodSteps.forEach((step) => {
          step.style.setProperty('--method-clone-step-opacity', '0');
          step.style.setProperty('--method-clone-step-x', '0vw');
          step.style.setProperty('--method-clone-step-y', '5.5rem');
          step.style.setProperty('--method-clone-step-focus', '0%');
          step.setAttribute('aria-hidden', 'true');
        });
      };

      if (methodSection.getBoundingClientRect().top > 0) {
        hideMethodSequence();
        return;
      }

      const scrollable = Math.max(1, sectionHeight - window.innerHeight);
      const sectionProgress = (window.scrollY - sectionTop) / scrollable;
      const rawProgress = Math.min(1, Math.max(0, sectionProgress));
      const ease = (value) => value * value * (3 - 2 * value);
      const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));
      const methodProgress = rawProgress;
      const titleSequenceEnd = 0.22;
      const stepSequenceStart = 0.3;
      const stepSequenceEnd = 0.88;
      const titleFadeStart = 0.895;
      const titleFadeEnd = 0.94;
      const lineWindow = titleSequenceEnd / Math.max(1, methodTitleLines.length);
      const stepsEnabled = methodProgress >= stepSequenceStart;
      const stepsProgress = clamp((methodProgress - stepSequenceStart) / (stepSequenceEnd - stepSequenceStart));
      const timeline = stepsProgress * methodSteps.length;
      const titleFade = 1 - ease(clamp((methodProgress - titleFadeStart) / (titleFadeEnd - titleFadeStart)));

      if (methodTitle) {
        methodTitle.style.setProperty('--method-clone-title-opacity', titleFade.toFixed(3));
      }

      const titleRect = methodTitle?.getBoundingClientRect();
      const navHeight = parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--nav-height')) || 0;
      const frameRect = methodFrame?.getBoundingClientRect();
      const frameStyle = methodFrame ? getComputedStyle(methodFrame) : null;
      const framePaddingLeft = frameStyle ? parseFloat(frameStyle.paddingLeft) || 0 : 0;
      const framePaddingRight = frameStyle ? parseFloat(frameStyle.paddingRight) || 0 : 0;
      const heroLeft = frameRect ? frameRect.left + framePaddingLeft : Math.max(16, titleRect?.left ?? 0);
      const heroRight = frameRect ? frameRect.right - framePaddingRight : window.innerWidth - heroLeft;
      const heroSafeInset = clamp(window.innerWidth * 0.03, 18, 48);
      const heroInnerLeft = heroLeft + heroSafeInset;
      const heroInnerRight = heroRight - heroSafeInset;
      const heroAvailableWidth = Math.max(1, heroInnerRight - heroInnerLeft);
      const heroCenterX = heroInnerLeft + heroAvailableWidth / 2;
      const getLineTextWidth = (line) => Math.max(1, line.offsetWidth || line.scrollWidth);
      const heroReferenceLine = methodTitleLines[methodTitleLines.length - 1] ?? methodTitleLines[0];
      const heroReferenceWidth = heroReferenceLine ? getLineTextWidth(heroReferenceLine) : 1;
      const sharedHeroScale = heroAvailableWidth / heroReferenceWidth;

      methodTitleLines.forEach((line, index) => {
        const local = (methodProgress - index * lineWindow) / lineWindow;
        const lineWidth = getLineTextWidth(line);
        const lineHeight = Math.max(1, line.offsetHeight);
        const lineLeft = (titleRect?.left ?? 0) + line.offsetLeft;
        const lineTop = (titleRect?.top ?? 0) + line.offsetTop;
        const heroScale = sharedHeroScale;
        const heroX = heroCenterX - (lineWidth * heroScale) / 2 - lineLeft;
        const heroY = navHeight + (window.innerHeight - navHeight - lineHeight * heroScale) / 2 - lineTop;
        const finalY = -index * lineHeight * 0.34;
        let opacity = 0;
        let travel = 1;
        let scale = heroScale;
        let accent = 0;

        if (local >= 0 && local < 0.72) {
          opacity = 1;
          travel = 1;
          scale = heroScale;
        } else if (local >= 0.72 && local < 1) {
          const settle = ease((local - 0.72) / 0.28);
          opacity = 1;
          travel = 1 - settle;
          scale = heroScale + (0.66 - heroScale) * settle;
        } else if (local >= 1) {
          opacity = 1;
          travel = 0;
          scale = 0.66;
          accent = ease(clamp((local - 1) / 0.1));
        }

        line.style.setProperty('--method-clone-line-opacity', opacity.toFixed(3));
        line.style.setProperty('--method-clone-line-x', `${(heroX * travel).toFixed(1)}px`);
        line.style.setProperty('--method-clone-line-y', `${(heroY * travel + finalY * (1 - travel)).toFixed(1)}px`);
        line.style.setProperty('--method-clone-line-scale', scale.toFixed(3));
        line.style.setProperty('--method-clone-line-accent', `${Math.round(accent * 100)}%`);
      });

      methodSteps.forEach((step, index) => {
        const local = stepsEnabled ? timeline - index : -1;
        let opacity = 0;
        let translateX = 0;
        let translateY = 5.5;
        let focus = 0;

        if (local >= 0 && local < 0.16) {
          const progress = ease(local / 0.16);
          opacity = progress;
          translateY = 5.5 * (1 - progress);
          focus = progress;
        } else if (local >= 0.16 && local < 0.78) {
          opacity = 1;
          translateY = 0;
          focus = 1;
        } else if (local >= 0.78 && local < 0.92) {
          const progress = ease((local - 0.78) / 0.14);
          opacity = 1 - progress;
          translateX = -46 * progress;
          translateY = 0;
          focus = 1;
        } else {
          opacity = 0;
          translateX = local < 0 ? 0 : -46;
          translateY = local < 0 ? 5.5 : 0;
          focus = local < 0 ? 0 : 1;
        }

        const active = opacity > 0.5;

        step.style.setProperty('--method-clone-step-opacity', opacity.toFixed(3));
        step.style.setProperty('--method-clone-step-x', `${translateX.toFixed(1)}vw`);
        step.style.setProperty('--method-clone-step-y', `${translateY.toFixed(2)}rem`);
        step.style.setProperty('--method-clone-step-focus', `${Math.round(focus * 100)}%`);
        step.setAttribute('aria-hidden', String(!active));
      });
    };

    let methodTicking = false;
    const requestMethodUpdate = () => {
      if (methodTicking) return;
      methodTicking = true;
      window.requestAnimationFrame(() => {
        updateMethodSteps();
        methodTicking = false;
      });
    };

    const onResize = () => {
      cacheSectionRect();
      fitMethodTitle();
      fitMethodStepTitles();
      requestMethodUpdate();
    };

    cacheSectionRect();
    fitMethodTitle();
    fitMethodStepTitles();
    updateMethodSteps();

    window.addEventListener('scroll', requestMethodUpdate, { passive: true });
    window.addEventListener('resize', onResize);

    if ('ResizeObserver' in window && methodTitle) {
      new ResizeObserver(onResize).observe(methodTitle);
    }

    // Scroll magnetism: snap to nearest title line or step when user pauses
    if (!reduceMethodMotion && methodSteps.length > 0) {
      const TITLE_END  = 0.22;
      const LINE_WIN   = TITLE_END / Math.max(1, methodTitleLines.length);
      const STEP_START = 0.3;
      const STEP_END   = 0.88;

      const scrollable = () => Math.max(1, sectionHeight - window.innerHeight);

      // Each title line peaks visually at local ≈ 0.36 (midpoint of 0–0.72 visible window)
      const getTitleSnapY = (i) =>
        sectionTop + (i + 0.36) * LINE_WIN * scrollable();

      // Each step peaks at local ≈ 0.47 (midpoint of 0.16–0.78 fully-visible window)
      const getStepSnapY = (i) => {
        const p = STEP_START + ((i + 0.47) / methodSteps.length) * (STEP_END - STEP_START);
        return sectionTop + p * scrollable();
      };

      // Find nearest Y from a list of candidates
      const nearestY = (candidates) =>
        candidates.reduce((best, y) =>
          Math.abs(window.scrollY - y) < Math.abs(window.scrollY - best) ? y : best,
          candidates[0]
        );

      let isSnapping = false;
      let snapTimer;

      const doSnap = () => {
        if (isSnapping) return;
        const sp = (window.scrollY - sectionTop) / scrollable();
        let targetY = null;

        if (sp >= LINE_WIN * 0.5 && sp < TITLE_END) {
          // Title zone: snap to nearest of the 3 lines
          // Lower bound (LINE_WIN*0.5) lets user exit the section freely at the top
          // Upper bound (TITLE_END=0.22) leaves the gap 0.22→0.30 as a free zone
          targetY = nearestY(methodTitleLines.map((_, i) => getTitleSnapY(i)));
        } else if (sp >= STEP_START && sp <= STEP_END) {
          // Step zone: snap to nearest of the 6 steps
          const t = ((sp - STEP_START) / (STEP_END - STEP_START)) * methodSteps.length;
          const idx = Math.min(methodSteps.length - 1, Math.max(0, Math.round(t - 0.47)));
          targetY = getStepSnapY(idx);
        }

        if (targetY === null || Math.abs(window.scrollY - targetY) < 30) return;

        isSnapping = true;
        window.scrollTo({ top: targetY, behavior: 'smooth' });
        setTimeout(() => { isSnapping = false; }, 700);
      };

      window.addEventListener('wheel', () => {
        if (isSnapping) return;
        clearTimeout(snapTimer);
        snapTimer = setTimeout(doSnap, 300);
      }, { passive: true });
    }
  }

</script>

<script>
  // DotField — vanilla JS canvas background for hero
  (function initDotField() {
    const TWO_PI = Math.PI * 2;
    const P = {
      dotRadius:    1.5,
      dotSpacing:   14,
      cursorRadius: 750,
      bulgeStrength: 67,
      glowRadius:   160,
      gradientFrom: '#eae2b7',
      gradientTo:   '#fcbf49',
      glowColor:    '#ffffff',
    };

    const container = document.getElementById('hero-dot-field');
    if (!container) return;

    // Canvas
    const canvas = document.createElement('canvas');
    canvas.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;';
    container.appendChild(canvas);
    const ctx = canvas.getContext('2d', { alpha: true })!;
    const dpr = Math.min(window.devicePixelRatio || 1, 2);

    // SVG glow overlay
    const glowId = 'dfg-' + Math.random().toString(36).slice(2, 7);
    const NS = 'http://www.w3.org/2000/svg';
    const svg = document.createElementNS(NS, 'svg');
    svg.style.cssText = 'position:absolute;inset:0;width:100%;height:100%;pointer-events:none;overflow:visible;';
    const defs = document.createElementNS(NS, 'defs');
    const rg   = document.createElementNS(NS, 'radialGradient');
    rg.setAttribute('id', glowId);
    const s1 = document.createElementNS(NS, 'stop');
    s1.setAttribute('offset', '0%');
    s1.setAttribute('stop-color', P.glowColor);
    s1.setAttribute('stop-opacity', '0.18');
    const s2 = document.createElementNS(NS, 'stop');
    s2.setAttribute('offset', '100%');
    s2.setAttribute('stop-color', P.glowColor);
    s2.setAttribute('stop-opacity', '0');
    rg.appendChild(s1); rg.appendChild(s2); defs.appendChild(rg); svg.appendChild(defs);
    const glowCircle = document.createElementNS(NS, 'circle');
    glowCircle.setAttribute('cx', '-9999');
    glowCircle.setAttribute('cy', '-9999');
    glowCircle.setAttribute('r', String(P.glowRadius));
    glowCircle.setAttribute('fill', `url(#${glowId})`);
    glowCircle.style.opacity = '0';
    svg.appendChild(glowCircle);
    container.appendChild(svg);

    // State
    type Dot = { ax: number; ay: number; sx: number; sy: number };
    let dots: Dot[] = [];
    let W = 0, H = 0, offX = 0, offY = 0;
    let mx = -9999, my = -9999, pmx = -9999, pmy = -9999, mspeed = 0;
    let glowOpacity = 0, engagement = 0;
    let rafId = 0, resizeTimer = 0;

    function buildGrid(w: number, h: number) {
      const step = P.dotRadius * 2 + P.dotSpacing;
      const cols = Math.floor(w / step);
      const rows = Math.floor(h / step);
      const padX = (w - cols * step) / 2 + step / 2;
      const padY = (h - rows * step) / 2 + step / 2;
      dots = [];
      for (let r = 0; r < rows; r++) {
        for (let c = 0; c < cols; c++) {
          const ax = padX + c * step;
          const ay = padY + r * step;
          dots.push({ ax, ay, sx: ax, sy: ay });
        }
      }
    }

    function doResize() {
      const rect = container.getBoundingClientRect();
      W = rect.width; H = rect.height;
      canvas.width  = W * dpr; canvas.height = H * dpr;
      canvas.style.width = W + 'px'; canvas.style.height = H + 'px';
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      offX = rect.left + window.scrollX;
      offY = rect.top  + window.scrollY;
      buildGrid(W, H);
    }

    function onResize() {
      clearTimeout(resizeTimer);
      resizeTimer = window.setTimeout(doResize, 80);
    }

    function onMouseMove(e: MouseEvent) {
      mx = e.pageX - offX;
      my = e.pageY - offY;
    }

    const speedTimer = setInterval(() => {
      const dx = pmx - mx, dy = pmy - my;
      const d = Math.sqrt(dx * dx + dy * dy);
      mspeed += (d - mspeed) * 0.4;
      if (mspeed < 0.001) mspeed = 0;
      pmx = mx; pmy = my;
    }, 20);

    function tick() {
      const cr = P.cursorRadius;
      const crSq = cr * cr;

      const targetEng = Math.min(mspeed / 5, 1);
      engagement += (targetEng - engagement) * 0.06;
      if (engagement < 0.001) engagement = 0;

      glowOpacity += (engagement - glowOpacity) * 0.08;
      glowCircle.setAttribute('cx', String(mx));
      glowCircle.setAttribute('cy', String(my));
      glowCircle.style.opacity = String(glowOpacity);

      ctx.clearRect(0, 0, W, H);

      const grad = ctx.createLinearGradient(0, 0, W, H);
      grad.addColorStop(0, P.gradientFrom);
      grad.addColorStop(1, P.gradientTo);
      ctx.fillStyle = grad;

      const eng = engagement;
      const r = P.dotRadius;

      ctx.beginPath();
      for (let i = 0; i < dots.length; i++) {
        const dot = dots[i];
        const dx = mx - dot.ax;
        const dy = my - dot.ay;
        const distSq = dx * dx + dy * dy;

        if (distSq < crSq && eng > 0.01) {
          const dist  = Math.sqrt(distSq);
          const t     = 1 - dist / cr;
          const push  = t * t * P.bulgeStrength * eng;
          const angle = Math.atan2(dy, dx);
          dot.sx += (dot.ax - Math.cos(angle) * push - dot.sx) * 0.15;
          dot.sy += (dot.ay - Math.sin(angle) * push - dot.sy) * 0.15;
        } else {
          dot.sx += (dot.ax - dot.sx) * 0.1;
          dot.sy += (dot.ay - dot.sy) * 0.1;
        }

        ctx.moveTo(dot.sx + r, dot.sy);
        ctx.arc(dot.sx, dot.sy, r, 0, TWO_PI);
      }
      ctx.fill();

      rafId = requestAnimationFrame(tick);
    }

    doResize();
    window.addEventListener('resize',    onResize,    { passive: true });
    window.addEventListener('mousemove', onMouseMove, { passive: true });
    rafId = requestAnimationFrame(tick);

    document.addEventListener('astro:before-swap', () => {
      cancelAnimationFrame(rafId);
      clearInterval(speedTimer);
      window.removeEventListener('resize',    onResize);
      window.removeEventListener('mousemove', onMouseMove);
    }, { once: true });
  })();


</script>

<script>
  import '../scripts/method-aurora.js';
</script>
